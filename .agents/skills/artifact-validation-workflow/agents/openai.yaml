interface:
  display_name: "Artifact Validation Workflow"
  short_description: "Validate artifacts with readiness gates"
  default_prompt: "Use $artifact-validation-workflow to validate this artifact and give a readiness verdict."
policy:
  allow_implicit_invocation: true
