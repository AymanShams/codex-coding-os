# Claude Code Entry Point

Follow `AGENTS.md` first. This repo uses the same source-of-truth and validation rules for Claude Code, Codex, and human contributors.

Before implementation:

1. Read `AGENTS.md`.
2. Read `docs/index.md`.
3. Read the latest handoff note under `docs/history/`.
4. Read the controlled TDD.
5. Run `git status -sb`.

Do not create a parallel plan that ignores the controlled product docs, TDD, security rules, or repo documentation.
