interface:
  display_name: "New Project Docs"
  short_description: "Run fail-closed project documentation workflows"
  default_prompt: "Use $new-project-documentation-system in Full Run mode. Create the workflow manifest first, stop for all unresolved material decisions, and do not recommend coding until every required phase passes."
