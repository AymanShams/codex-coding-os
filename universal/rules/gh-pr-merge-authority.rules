# BEGIN CODEX CODING OS MANAGED: CAMPAIGN EXTERNAL EFFECTS
prefix_rule(
    pattern = ["gh", "pr", ["create", "merge"]],
    decision = "prompt",
    justification = "Automated pull-request mutations must use the campaign engine outbox and exact-head reconciliation.",
)
prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Automated pushes must use the campaign engine outbox and stable operation identity.",
)
# END CODEX CODING OS MANAGED: CAMPAIGN EXTERNAL EFFECTS
